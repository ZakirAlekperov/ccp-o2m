from django.apps import AppConfig


class GroundStationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ground_stations'
    verbose_name = 'Наземные станции'
